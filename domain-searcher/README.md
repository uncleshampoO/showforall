# 🌐 Domain Searcher SaaS

Масштабируемая система для фонового парсинга `expireddomains.net` с обходом анти-бот защит Cloudflare на базе Playwright, FastAPI и кастомного Chrome Extension.

***

## ⚠️ Архитектурное Решение: Обход Cloudflare (Session Stealing)

**Проблема:** Классические инструменты парсинга (даже с использованием `playwright-stealth`) часто блокируются при попытке автоматизировать процесс авторизации (Login) из-за систем защиты от ботов, таких как **Cloudflare Turnstile**. Целевой сервис активно выявляет и пресекает работу Headless-браузеров на этапе входа в аккаунт.

(Решение): Вместо попыток "пробить лбом" форму авторизации Headless-браузером, мы применили архитектуру **"Micro-SaaS с инъекцией сессии (Session Stealer)"**:

1.  **Делегирование авторизации (Живой Браузер)**: Вход в аккаунт и прохождение капчи происходят в обычном браузере пользователя (или менеджера). Cloudflare доверяет реальной сессии.
2.  **Перехват сессии (Chrome Extension)**: Написано легковесное расширение (UI-дашборд), которое вытаскивает `Cookies` авторизованного аккаунта напрямую из браузера Chrome и отправляет их в базу данных сервера.
3.  **Автономный парсинг (FastAPI + Playwright Worker)**: Сервер подхватывает "живую" сессию из базы и скармливает её Headless-браузеру. Далее бот имитирует поведение человека (микро-скроллы, задержки, ручные клики по UI) и собирает данные, оставаясь незамеченным.

Такой гибридный подход позволяет полностью устранить риски блокировки на самом уязвимом этапе (логин) и дает возможность масштабировать систему (менеджеры могут закидывать свои сессии на один центральный сервер).

***

## ⚙️ Установка и запуск

### 1. Запуск Бэкенда в 1 клик
Мы максимально упростили запуск сервера. Больше не нужно вручную вводить команды терминала.

**Для Windows:**
Просто дважды кликните по файлу `start_windows.bat` в папке проекта.
*(Скрипт сам создаст виртуальное окружение, установит все библиотеки, скачает браузеры Playwright и запустит FastAPI сервер).*

**Для Mac/Linux:**
Откройте терминал и запустите:
```bash
bash start.sh
```

**Сервер успешно запущен, если в консоли вы видите:** `Uvicorn running on http://127.0.0.1:8000`

### 2. Установка Клиента (Chrome Extension)

В рамках тестового задания расширение устанавливается локально (в "режиме разработчика"). 
*(В реальном Production для рядовых менеджеров расширение просто публикуется в приватный доступ в Chrome Web Store, и устанавливается по ссылке в 1 клик).*

**Инструкция для локальной установки (Тестирование):**
1. Откройте браузер Google Chrome и введите в адресную строку: `chrome://extensions/`
2. В правом верхнем углу включите тумблер **"Режим разработчика"** (Developer mode).
3. Слева вверху появится кнопка **"Загрузить распакованное расширение"** (Load unpacked). Нажмите её.
4. Выберите папку `extension`, которая находится внутри папки распакованного проекта.
5. Расширение появится в списке. Рекомендуем нажать на значок "пазла" в правом верхнем углу браузера и закрепить (Pin) иконку расширения на панели для удобства.

***

## 🚀 Воспроизведение (Как тестировать решение)

1.  Откройте сайт [expireddomains.net](https://expireddomains.net) в Chrome и авторизуйтесь под своим аккаунтом вручную. Решите капчу, если потребуется.
2.  Кликните на иконку установленного расширения **Domain Searcher Extension**. Вы увидите минималистичный интерфейс панели управления.
3.  Нажмите кнопку **"🔗 Синхронизировать доступ"**. Расширение автоматически вытащит вашу сессию с сайта и передаст серверу. Кнопка загорится зеленой галочкой.
4.  В поле `Лимит поиска` укажите целевое количество доменов (например, `10`).
5.  Нажмите **"▶ Запустить Поиск"**.

**Что произойдет под капотом:**
*   Сервер поднимет фоновый браузер с вашей сессией.
*   Бот перейдет в `Deleted .com`, отфильтрует контент от стоп-слов.
*   Бот *эмулирует клик* по заголовку таблицы `BL`, чтобы отсортировать выдачу (по ТЗ).
*   Бот *эмулирует клик* по кнопке `Next`, чтобы начать сбор только со 2-й страницы (по ТЗ).
*   Все домены проверяются на доступность через официальное **RDAP API реестра Verisign** (замена старому WHOIS-сокетам).
*   Результат выводится в Дашборд расширения в реальном времени с прогресс-баром.
*   По завершению, домен можно копировать по клику, а список — экспортировать в CSV.

***

## 🛠 Реализованные требования из ТЗ

✅ **Авторизация**: Реализована безопасная инъекция сессии через Extension.
✅ **Поиск**: Переход в `Deleted .com`, фильтр возраста (>= 5 лет), фильтр по триггерным словам (маты, казино и пр. — см. `core/filters.py`).
✅ **Сортировка**: Данные сортируются по метрике `BL` от большего к меньшему (путем UI-клика в парсере).
✅ **Логика отбора**: Бот стартует со 2-й страницы, пропуская первую. Проверка занятости реализована через надежное RDAP HTTP API (`core/verifier.py`).
✅ **Выдача**: Список доменов в расширении; click-to-copy; прогресс-бар; кнопка "Экспорт в CSV".
